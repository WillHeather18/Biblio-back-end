version: 1.0

runtime: nodejs20 # This should specify the runtime environment correctly

build:
  commands:
    - npm install # Install npm dependencies

run:
  command: "npm start" # Command to start the application
